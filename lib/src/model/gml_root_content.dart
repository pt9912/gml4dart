part of '../gml4dart_base.dart';

/// Marker interface for elements that can appear as the
/// root content of a [GmlDocument].
sealed class GmlRootContent {
  /// Creates a [GmlRootContent].
  const GmlRootContent();
}
